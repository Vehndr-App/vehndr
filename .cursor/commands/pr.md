You are preparing a PR for this repository.
*   Create a new git branch with a descriptive name based on the changes (e.g., `fix/user-auth` or `feat/dashboard`).
*   Stage all changes and commit them with a concise, descriptive message.
*   Push the branch to origin and use the `gh pr create` command to open a pull request. 
   - Generate a high-quality PR title
   - Add a **Description** section with a short high-level description of the PR 
   - Add a **Changes** section to summarize changes in a bulleted list (maximum 5 bullets).
   - List any potential risks and the rollback plan (1-2 bullets) in a **Potential Risks** section if needed.
   - If the diff is > 500 lines, propose how to split the PR into smaller, manageable chunks.
   - Tell reviewers exactly what to focus on during their review.
*   Provide the final PR URL once complete.